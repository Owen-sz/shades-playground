builder: dnf

import:
  - ../chromebook.yaml
  - ../../../xfce/xfce-live.yaml
  - ../../../xfce/base-disk-x86_64.yaml

distro: Ultramarine Linux 42 Xfce (Chromebook Edition)

out_file: ultramarine-cbe-xfce-42.img

bootloader: grub

kernel_cmdline: "quiet rhgb"

dnf:
  releasever: 42
  exec: dnf4
