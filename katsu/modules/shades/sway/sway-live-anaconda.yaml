builder: dnf

import:
  - ../base/base.yaml
  - ../base/desktop.yaml
  - ../live/live.yaml
  - ../live/anaconda.yaml
  - sway.yaml

distro: Ultramarine Linux 42 (Sway Shade)

out_file: ultramarine-shade-sway-42-live-anaconda.iso

scripts:
  post:
    - id: sway-live
      name: Set up Sway Live
      file: ./sway-live.sh
    - id: sway-live-anaconda
      name: Set up Sway Live (for Anaconda)
      file: ./sway-live-anaconda.sh

bootloader: grub

kernel_cmdline: "quiet rhgb"

iso:
  volume_id: UM-sway-Live-Anaconda

dnf:
  releasever: 42
  exec: dnf4
